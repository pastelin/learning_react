import { createContext } from 'react';

// Permite saber la informacion que se colocara
// Para que el hook de react busque este contexto y definir el proveedor
export const UserContext = createContext();